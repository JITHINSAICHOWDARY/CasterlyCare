# URL of the backend API (no trailing slash)
VITE_API_URL=http://localhost:5000

# Google Sign-In client ID (OAuth client, type "Web application").
# Must match GOOGLE_CLIENT_ID in backend/.env. Leave blank to hide the button.
VITE_GOOGLE_CLIENT_ID=
